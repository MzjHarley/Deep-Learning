 ```
test-argparse.py [-h] [--a D] [--b B] [--verbose] {+,-,*,/}

hello

positional arguments:
  {+,-,*,/}

optional arguments:
  -h, --help  show this help message and exit
  --a D       operation a
  --b B       operation b
  --verbose   print or non-print
```
